tasks:
- id: display-model-tier
  objective: |
    Add a `Display` trait implementation for the `ModelTier` enum in
    `crates/router/src/chooser.rs`. The output should be the variant name
    as a lowercase string: "economy", "standard", "premium". This matches
    the `from_str_loose` parsing already present. Add a unit test in the
    existing `#[cfg(test)] mod tests` block that verifies all three variants
    produce the expected string via `.to_string()`.
  priority: 10
  status: completed
  depends_on: []
- id: display-task-status
  objective: |
    Add a `Display` trait implementation for the `TaskStatus` enum in
    `crates/eng-org/src/taskqueue.rs`. Use snake_case output matching the
    serde rename: "pending", "in_progress", "completed", "failed", "skipped".
    Add a unit test in the existing test module that verifies all five
    variants produce the correct string via `.to_string()`.
  priority: 20
  status: completed
  depends_on: []
- id: display-cease-reason
  objective: |
    Add a `Display` trait implementation for the `CeaseReason` enum in
    `crates/eng-org/src/orchestrator.rs`. Use human-readable output:
    "all tasks done", "budget exhausted", "quality gate failed". Add a
    unit test that verifies all three variants produce the correct string.
    The enum is defined around line 234. Use `read_file` with `start_line`
    and `end_line` to read only the relevant section (~lines 220-250).
  priority: 30
  status: pending
  depends_on: []
