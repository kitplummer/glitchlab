use std::path::Path;

use anyhow::Result;

pub async fn execute(repo: &Path) -> Result<()> {
    println!("glitchlab batch — not yet implemented");
    println!("repo: {}", repo.display());
    Ok(())
}
